apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dnsmasq
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 128M
  storageClassName: microk8s-hostpath
  volumeMode: Filesystem
